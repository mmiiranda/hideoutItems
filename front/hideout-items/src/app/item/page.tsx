import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Hideout Items | Item",
    description: "Page of Item"
}

export default function Item(){
    return(
        <h1>Item</h1>
    )
}